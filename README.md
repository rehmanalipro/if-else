# if-else
This Dart program takes two numbers as input from the user through the terminal, checks whether each number is even or odd using the modulus operator (%), and then compares both values to determine if they are equal.
